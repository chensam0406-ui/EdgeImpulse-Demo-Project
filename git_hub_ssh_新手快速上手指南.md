# GitHub SSH 新手快速上手指南（一次設定，之後免煩惱）

> 本文件適合 **第一次使用 GitHub + SSH 的新手**，請照順序操作即可完成所有設定。

---

## 一、建立 SSH 金鑰（只要做一次）

在終端機輸入以下指令（請把信箱換成你的 GitHub 信箱）：

```bash
ssh-keygen -t rsa -b 4096 -C "你的GitHub信箱"
```

接下來會出現幾個提示：

### 1️⃣ Enter file in which to save the key:
👉 **直接按 Enter**（使用預設路徑 `~/.ssh/id_rsa`）

### 2️⃣ Enter passphrase:
👉 **可直接按 Enter**（新手建議先不要設密碼）

完成後，你會得到兩個檔案：

- `~/.ssh/id_rsa` → **私鑰（非常重要，不能外流）**
- `~/.ssh/id_rsa.pub` → **公鑰（要給 GitHub）**

---

## 二、把公鑰加到 GitHub（只要一次）

### 1️⃣ 顯示你的公鑰

```bash
cat ~/.ssh/id_rsa.pub
```

👉 **全部複製**（通常尾巴會是你的信箱）

### 2️⃣ 到 GitHub 設定

1. 打開 GitHub
2. 進入 **Settings → SSH and GPG keys**
3. 點擊 **New SSH key**

填寫：
- **Title**：隨意（例如：`WSL-Laptop`）
- **Key**：貼上剛剛複製的公鑰

點擊 **Save**

---

## 三、設定「開機自動啟用 SSH 金鑰」（重點）

這一步可以讓你 **每次開終端機都自動載入 SSH 金鑰**，不用重複輸入。

### 1️⃣ 編輯 `.bashrc`

```bash
nano ~/.bashrc
```

### 2️⃣ 移動到檔案最底部，貼上以下內容

```bash
if [ -z "$SSH_AUTH_SOCK" ]; then
    eval "$(ssh-agent -s)" >/dev/null
    ssh-add -q ~/.ssh/id_rsa 2>/dev/null
fi
```

### 3️⃣ 儲存並離開

- `Ctrl + O` → `Enter`
- `Ctrl + X`

### 4️⃣ 重新載入設定

```bash
source ~/.bashrc
```

---

## 四、驗證 SSH 是否設定成功（一定要做）

### 1️⃣ 檢查金鑰是否已載入

```bash
ssh-add -l
```

看到類似以下輸出代表成功：

```
4096 SHA256:xxxxxx ~/.ssh/id_rsa
```

### 2️⃣ 測試 GitHub 連線

```bash
ssh -T git@github.com
```

第一次會詢問是否信任，請輸入：

```text
yes
```

如果看到以下訊息，表示 **SSH 完全成功** 🎉

```text
Hi chensam0406-ui! You've successfully authenticated...
```

---

## 五、安裝 Git 並設定使用者資訊

### 1️⃣ 安裝 Git

```bash
sudo apt update && sudo apt install git -y
```

### 2️⃣ 設定 Git 使用者資訊（一定要設定）

```bash
git config --global user.email "你的GitHub信箱@example.com"
```

```bash
git config --global user.name "你的英文名字"
```

⚠️ **雙引號請勿刪除**

---

## 六、Clone 專案並初始化環境

### 1️⃣ Clone 專案

```bash
git clone git@github.com:chensam0406-ui/EdgeImpulse-Demo-Project.git
```

### 2️⃣ 進入專案資料夾後，初始化環境

```bash
./scripts/setup_env.sh
```

👉 過程中會要求輸入 **系統密碼**（正常）

### 3️⃣ 推送測試（確認流程正常）

```bash
./scripts/push.sh
```

若成功執行，代表：

✅ SSH 設定完成  
✅ Git 設定完成  
✅ 專案環境建置完成

---

🎉 **恭喜！你已完成新人所有必要設定，可以正式開始開發了！**

